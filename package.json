{
  "name": "get-current-time",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "moment-timezone": "^0.5.45"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  },
  "scripts": {
    "start": "npm run build",
    "build": "ncc build index.js -m",
    "test": "jest --coverage"
  }
}
