{
  "name": "get-current-time",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "moment-timezone": "^0.5.44"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@vercel/ncc": "^0.36.1",
    "jest": "^29.4.1"
  },
  "scripts": {
    "start": "npm run build",
    "build": "ncc build index.js -m",
    "test": "jest --coverage"
  }
}
